# What is libcxsh?
A library for toolchain development

# Install guide
You just need to put these commands:
```
make
sudo make install
```
if you don't have root permission, you can use the static library at build/libcxsh.a without installing

# Notes
- Documentation is WIP.
